#include <stdio.h>
#include <math.h>
int main(){
    float a, b, c, s, area;
    printf("Type the sides of the triangle\t");
    scanf("%f %f %f", &a, &b, &c);
    if(a + b < c)
        printf("Triangle not possible");
    else if(a + c < b)
        printf("Triangle not possible");
    else if(b + c < a)
        printf("triangle not possible");
    else
        s = (a + b + c)/2;
        area = sqrt(s* (s-a) * (s-b) * (s-c));

        printf("\nThe area is %f", area);
        return 0;





}
